extend schema
  @link(
    url: "https://specs.apollo.dev/federation/v2.1"
    import: ["@composeDirective", "@external", "@key"]
  )

extend schema @link(url: "https://specs.apollo.dev/link/v1.0")

type whose602 @key(fields: "scornf32") {
  scornf32: String! @external
  circa4cb: String
}
